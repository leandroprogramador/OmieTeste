<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OmieTeste"
        tools:targetApi="31">
        <activity
            android:name=".ui.pedido.PedidoDetalhesActivity"
            android:exported="false"
            android:label="@string/detalhe_pedido"
            />
        <activity
            android:name=".ui.pedido.MeusPedidosActivity"
            android:exported="false"
            android:label="@string/meus_pedidos" />
        <activity
            android:name=".ui.pedido.NovoPedidoActivity"
            android:exported="false"
            android:label="@string/nova_venda" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.OmieTeste">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>